services:
  cloudflare-ddns:
    build:
      context: ../Powershell Alpine Base
      dockerfile: dockerfile
    image: cloudflare-ddns:alpine
    container_name: cloudflare-ddns
    restart: unless-stopped
    env_file:
      - .env
